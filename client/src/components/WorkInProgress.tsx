import React from "react";

const WorkInProgress: React.FC = (): ReturnType<React.FC> => {
  return (
    <div
      style={{
        color: "white",
        fontSize: 32,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        padding: 32,
      }}
    >
      Not yet implemented
    </div>
  );
};

export default WorkInProgress;

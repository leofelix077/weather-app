import { LOCALE_KEY } from "../constants";

describe("API call tests", () => {
  it("should succeed", () => expect(LOCALE_KEY).toBeDefined());
});

describe("component rendering", () => {
  it("should succeed", () => expect(LOCALE_KEY).toBeDefined());
});
